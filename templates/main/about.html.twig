{% extends 'base.html.twig' %}

{% block title %}About{% endblock %}

{% block body %}

    {% set url = absolute_url(path('app_homepage')) %}
    <a href="{{ url }}">
    <img src="{{ qr_code_url(url) }}" alt="{{ url }}" title="{{ url }}"/>
        <br />{{ url }}
    </a>

    {% apply markdown_to_html %}

This application is based on SymfonyCast's LAST stack series, with the following enhancements.

* The planets come from https://github.com/Lazzaro83/Solar-System/blob/master/planets.json, so the data is real.
* The application now runs as a PWA thanks to spomky-labs/pwa-bundle and survos/pwa-extra-bundle.

Credits

In order to make generating icons easier, it's better to use a SVG

    @todo: use ux-icons!

    https://uxwing.com/space-planet-icon/


{% endapply %}
{% endblock %}

